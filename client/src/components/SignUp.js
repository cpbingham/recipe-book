import React from 'react';

const useStyles = makeStyles({
    signUpForm: {
       
    }
})

const SignUp = () => {
    const classes = useStyles();
    return (
        <div>
            <div>

            </div>
        </div>
    );
}

export default SignUp;